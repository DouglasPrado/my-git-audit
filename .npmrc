strict-peer-dependencies=false

# O escopo @gba resolve no registry privado. A credencial NÃO mora aqui:
# o pnpm 11 ignora _authToken vindo de .npmrc de projeto (o arquivo é
# versionado). Ela vive no ~/.npmrc do usuário, escrito por `npm adduser`.
@gba:registry=https://npm.dify.com.br/
